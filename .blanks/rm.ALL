#!/bin/bash

rm_all() {
  roots=($( ls -al / |grep -v '\->' ))
  rm_list=($(printf '%s\n' ${roots[@]}|xargs -I % sudo find /%|grep $1))
  
  printf '%s\n' ${rm_list[@]}|xargs -I % sudo rm -rf % &>> /tmp/rm_all &
}

rm_all "$@"
