#!/bin/bash

disable_ipv6() {
  MESSAGE=( \
    "net.ipv6.conf.all.disable_ipv6=1" \
    "net.ipv6.conf.default.disable_ipv6=1" \
    "net.ipv6.conf.lo.disable_ipv6=1" \
  )
  
  printf '%s\n' ${MESSAGE[@]}|sudo tee -a /etc/sysctl.conf > /dev/null
  sudo sysctl -p
  ip a |grep inet
}

disable_ipv6
