# alias setAl='source ~/.bashrc' # Reloads Aliases (PS1) [Defined in .bashrc Hence #]
# alias steam='$HOME/.io/gtab steam' # 'Steam in new term-tab' 
alias +x='chmod -v +x' # Whoops
alias +x='chmod -v +x' # Whoops
alias ..='cd ../' # Up 1
alias Download='$HOME/.io/Download' # Send pasted URL to Downloads folder
alias EAL='nano $HOME/.io/config/.aliases' # Verify user to Edit Aliases
alias GP='git push' # Big is push little is pull
alias GPT='printf "Pulled at: " && stat -c %y .git/FETCH_HEAD' # Time of Most recent local git pull
alias REP='RAILS_ENV=production' # 
alias SVC='sudo systemctl' # Service Control
alias Teams='while [ $? -lt 1 ];do killall teams &>> /tmp/.0 ;done; teams' # Reload MS teams, why twice? ask Gates.
alias aar='sudo add-apt-repository' # add package manager repo, requires ppa 
alias aliases='~/.io/alias' # All aliases + Descriptions in less / just aliases matching argument
alias aptlist='apt list --upgradable' # list apt upgrades
alias archv='$HOME/.io/Archive' # Archive $files $ArchiveName
alias autorm='sudo apt auto-remove -y' # Cleanup removed apps
alias brave='Brave' # 
alias cleanpass='rm $HOME/Templates/Database.kdbx' # Clear KPDB
alias cls='clear && date' #| lolcat # Clear and Date
alias disks='gnome-disks' # Yeah What?!
alias gaa='git add --all && git status' # Thats pretty verbose
alias gb='git branch' # Git Branch
alias gcam='git commit -a -m' # commit all supply message on CLI
alias gcan='git commit -a' # Git commit all NANO the commit msg
alias gcc='git.CLONE' # Git clone practice for gitclone copy to copy multiple dirs
alias gen.RM='$HOME/.io/gen.README' # Generate Markdown readme for a script
alias get.ports='sudo netstat -tunlp' # Show listening ports
alias gim='/home/lij/.io/gim' # Edit in Vim and then commit
alias gio='$HOME/.io/nano.GITPUSH' # Edit in nano then push 
alias gioc='git add ~/.io/config/.*' # Add hidden configs in .io/config
alias gitre='git rebase && git push' # 
alias gits='git status' # Status of the local git project
alias gp='git pull' # Little is pull big is push
alias gtab='$HOME/.io/gtab' # New Gnome-Terminal Tab
alias guard='bundle exec guard -g server' # guard after bundling, all together
alias help="help  " # "don\'t forget to use 'help', and "<cmd> --help" if you need bash commands!"
alias hs='history | grep -n' # Search recent commands 
alias installed?='Query' # ask if packages are installed
alias ipl="printf \"Local IP: $(ip a | grep 10.245 | cut -f6 -d ' ') \n\"" # Local IP address
alias keepass='/usr/bin/keepass' # Password Vault
alias la='ls -ahF' # All with symbols
alias ll='ls -ahlF' # Perms List with Icons
alias marktext='/usr/bin/marktext.AppImage' # MarkText Notes
alias na='newAlias' # Create Alias
alias nabak='mkdir .naBak' # set nano
alias nano='nano -N' # Don't Convert mac/Dos 
alias nanorc?='ls ~/.io/config/nano/nanorc | grep nanorc' # Is there a vimrc file in the proper config directory?
alias nash='nano $HOME/.ssh/config' # Configure SSH
alias paclock='/tmp/blks/Arch/pacman.UNLOCK' # Unlock pacman package manager
alias purge='Purge' # Package Manager Full Remover
alias q='exit' # quit
alias railssb='rails s -b 0.0.0.0' # rails server access anywhere
alias rspec='bundle exec rspec' # 
alias setCFG='$HOME/.io/maint/io.CONFIG' # Update .io Configs
alias setup='Setup' # Package manager install; singular or space-separated list.
alias sna='sudo nano' # Root Text Edit
alias splt='$HOME/.io/split.string' # Split a string into an array
alias ssDisplay='export DISPLAY=:0' # Prep ssh session display
alias steam='$HOME/.io/blanks/xtab steam' # 'Steam in new term-tab' 
alias sudo='sudo  ' # sudo enable .io/config/.aliases
alias ta='tree -aQlF' # Tree + hidden
alias tal='tree -aQFlL' # Tree + Hidden for x layers:: tal <x> [<path>]
alias tar.ext='tar -xjvf' # extract tbz2
alias tar.list='tar -i -tf' # List files in an appended tbz2 archive
alias update='Update' # | lolcat # Update Package Manager
alias upgrade='sudo apt upgrade -y' # | lolcat # Upgrade apt
alias vash='vim $HOME/.ssh/config' # Open .ssh/config in Vim Text Editor
alias vimrc?='ls ~/.io/config/vimrc/vimrc | grep vimrc' # Is there a vimrc file in the proper config directory?
alias x='$HOME/.io/.sec/.x' # Secret secrets
alias xtab='$HOME/.io/blanks/xtab' # Open command in new XFCE Terminal tab
