alias ack='ag' # '`faster` ack'
alias cat='bat' # 'use bat instead'
alias vi='$EDITOR'
alias nlf='$EDITOR ~/.bin/fns' # 'Edit local functions'
alias st='start_teams' # ''
alias ns='notify-send' # ''
alias grr='git remote remove origin' # ''
alias grab='git remote add -m' # 'add a remote to a branch. grab <branch> <name> <url>'
alias ybd='yarn build && yarn deploy' # 'redeploy'
